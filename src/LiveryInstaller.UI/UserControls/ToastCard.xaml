<UserControl x:Class="LiveryInstaller.UI.UserControls.ToastCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="Self"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <Style TargetType="{x:Type TextBlock}" x:Key="TitleStyle">
                <Setter Property="FontSize" Value="18" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>
            <Style TargetType="{x:Type TextBlock}" x:Key="DescriptionStyle">
                <Setter Property="FontSize" Value="14" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Margin="0,0,0,8"
            CornerRadius="6">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2"
                              Direction="270" Opacity="0.25" />
        </Border.Effect>
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="#2196F3" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Level, ElementName=Self}" Value="Success">
                        <Setter Property="Background" Value="#4CAF50" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Level, ElementName=Self}" Value="Warning">
                        <Setter Property="Background" Value="#FF9800" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Level, ElementName=Self}" Value="Error">
                        <Setter Property="Background" Value="#F44336" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Margin="8,8,0,8"
                    CornerRadius="4"
                    Background="#30FFFFFF"
                    Padding="10,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" Width="25" Height="25" >
                        <Image.Style>
                            <Style TargetType="Image">
                                <Setter Property="Source" Value="pack://application:,,,/LiveryInstaller.UI;component/Images/info.png" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Level, ElementName=Self}" Value="Success">
                                        <Setter Property="Source" Value="pack://application:,,,/LiveryInstaller.UI;component/Images/success.png" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Level, ElementName=Self}" Value="Warning">
                                        <Setter Property="Source" Value="pack://application:,,,/LiveryInstaller.UI;component/Images/warning.png" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Level, ElementName=Self}" Value="Error">
                                        <Setter Property="Source" Value="pack://application:,,,/LiveryInstaller.UI;component/Images/error.png" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>
                    <StackPanel Grid.Column="1" Margin="5, 0, 0, 0">
                        <TextBlock Text="{Binding Title, ElementName=Self}"
                                   Visibility="{Binding TitleVisibility, ElementName=Self, Converter={StaticResource BooleanToVisibility}}"
                                   VerticalAlignment="Center"
                                   Foreground="White"
                                   Style="{StaticResource TitleStyle}" />
                        <TextBlock Text="{Binding Description, ElementName=Self}"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Center"
                                   Foreground="White"
                                   Style="{StaticResource DescriptionStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Dismiss -->
            <Button Grid.Column="1"
                    Content="✕"
                    Command="{Binding CloseCommand, ElementName=Self}"
                    Margin="4,8,8,8"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    Foreground="White" />
        </Grid>
    </Border>
</UserControl>