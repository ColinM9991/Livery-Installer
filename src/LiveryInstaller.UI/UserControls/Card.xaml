<UserControl x:Class="LiveryInstaller.UI.UserControls.Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             x:Name="Self"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" Background="Transparent">
    <Border CornerRadius="8" Background="#2D2D2D">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" MinWidth="600" Margin="18, 18, 18, 18">
                <TextBlock Text="{Binding Title, ElementName=Self}"
                           Visibility="{Binding TitleVisibility, ElementName=Self}"
                           FontSize="20" FontWeight="SemiBold" Foreground="#FFFFFF"
                           Margin="0, 0, 0, 12" />
                
                <TextBlock Text="{Binding Description, ElementName=Self}"
                           Visibility="{Binding DescriptionVisibility, ElementName=Self}"
                           FontSize="14" Foreground="#FFFFFF" Margin="0, 0, 0, 24"/>
            </StackPanel>
            
            <ContentPresenter Grid.Row="1" Content="{Binding CardContent, ElementName=Self}" />
            
            <ContentPresenter Grid.Row="2" Content="{Binding CardFooter, ElementName=Self}"
                              Visibility="{Binding CardFooterVisibility, ElementName=Self}" />
        </Grid>
    </Border>
</UserControl>