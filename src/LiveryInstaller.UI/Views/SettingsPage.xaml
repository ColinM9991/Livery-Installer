<UserControl x:Class="LiveryInstaller.UI.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:LiveryInstaller.UI.ViewModels"
             xmlns:uc="clr-namespace:LiveryInstaller.UI.UserControls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:SettingsPageViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="16,24,16,24" MinWidth="600">
                <TextBlock Text="Settings" Style="{StaticResource PageHeading}" />

                <uc:InputControl Icon="📂" Title="Liveries path"
                                 Description="Select the directory containing all raw PTP files">
                    <uc:InputControl.CardContent>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <uc:FilePickerControl Path="{Binding LiveriesPath, Mode=TwoWay}" Type="Folder"  />
                        </Grid>
                    </uc:InputControl.CardContent>
                </uc:InputControl>

                <!-- <uc:InputControl Icon="🗝️" Title="Decryption key" Description="The decryption key for the PTP files"> -->
                <!--     <uc:InputControl.CardContent> -->
                <!--         <TextBox Text="{Binding DecryptionKey}" /> -->
                <!--     </uc:InputControl.CardContent> -->
                <!-- </uc:InputControl> -->

                <uc:InputControl Icon="📝" Title="Log level" Description="The logging level for log events">
                    <uc:InputControl.CardContent>
                        <ComboBox SelectedValue="{Binding LogLevel}" ItemsSource="{Binding AvailableLogLevels}" />
                    </uc:InputControl.CardContent>
                </uc:InputControl>
            </StackPanel>
            <Button Grid.Row="1" Content="Save" HorizontalAlignment="Right" Margin="0,0,16,12"
                    Command="{Binding SaveSettingsCommand}" />
        </Grid>
    </ScrollViewer>
</UserControl>