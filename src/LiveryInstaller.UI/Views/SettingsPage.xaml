<UserControl x:Class="LiveryInstaller.UI.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:LiveryInstaller.UI.ViewModels"
             xmlns:uc="clr-namespace:LiveryInstaller.UI.UserControls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:SettingsPageViewModel}"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="16,24,16,24" MinWidth="600">
                <TextBlock Text="Settings" FontSize="28" FontWeight="SemiBold" Foreground="#FFFFFF"
                           Margin="0, 0, 0, 24" />

                <uc:SettingsCard Icon="📂" Title="Liveries path"
                                 Description="Select the directory containing all raw PTP files">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding LiveriesPath}" />
                        <Button Grid.Column="1" Height="36" Content="🔎" Margin="5, 0, 0, 0" Command="{Binding FolderPickerCommand}" ToolTip="Select the directory containing the liveries" />
                    </Grid>
                </uc:SettingsCard>

                <uc:SettingsCard Icon="🗝️" Title="Decryption key" Description="The decryption key for the PTP files">
                    <TextBox Text="{Binding DecryptionKey}" />
                </uc:SettingsCard>

                <uc:SettingsCard Icon="📝" Title="Log level" Description="The logging level for log events">
                    <ComboBox SelectedValue="{Binding LogLevel}" ItemsSource="{Binding AvailableLogLevels}" />
                </uc:SettingsCard>
            </StackPanel>
            <Button Grid.Row="1" Content="Save" HorizontalAlignment="Right" Margin="0,0,16,12"
                    Command="{Binding SaveSettingsCommand}" />
        </Grid>
    </ScrollViewer>
</UserControl>